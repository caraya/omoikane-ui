import { describe, it, expect } from 'vitest';
import * as Module from '../ProgressSpinner';

describe('ProgressSpinner', () => {
  it('loads module', () => {
    expect(Module).toBeTruthy();
  });
});
