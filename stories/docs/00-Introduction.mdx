import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Introduction" />

# Omoikane UI

This is a React component library designed to provide a consistent, accessible, and themeable set of UI primitives and molecules for building web applications.

It is intended as part of the Omoikane framework, but can be used standalone in any React project.